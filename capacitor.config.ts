import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kopera.condominio',
  appName: 'AdminCondominio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
